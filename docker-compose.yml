services:
  backend:
    build: ./a3-backend
    ports:
      - "8080:8080"
    volumes:
      - ./a3-backend:/app/src # Mapeia o diretório do projeto para /app no container
      - ./database:/app/database # Mapeamento do banco de dados SQLite
  frontend:
    build: ./a3-frontend
    ports:
      - "80:80"
    volumes:
      - ./a3-frontend:/usr/share/nginx/html